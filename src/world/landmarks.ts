// Fixed local-frame anchor points for the three authored special
// structures. They sit inside the Gaza City / Palestine Square real tile
// (which loads eagerly), a short walk from the world origin (0,0) — close
// enough together that the very first exploration loop touches all three.
export const GUIDE_LOCAL: [number, number] = [14, 22];
export const PORTAL_LOCAL: [number, number] = [30, 30];
export const COMMAND_CENTER_LOCAL: [number, number] = [-20, -14];

export const INTERACT_RADIUS_M = 6;
export const DISCOVERY_RADIUS_M = 16;
